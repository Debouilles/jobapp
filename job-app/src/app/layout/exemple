// angular.module('myApp', [])
// .controller('MyController', ['$http', function($http) {
//   var vm = this;
//   vm.services = [];

//   // Récupération des données de l'API
//   $http.get('https://jobapp.onrender.com/api/services').then(function(response) {
//     // Stockage des données dans un tableau
//     vm.services = response.data;
//   });
// }]);

<!-- 
<ul>
<li ng-repeat="service in vm.services">{{service.name}}</li>
</ul> -->